{% extends 'base.html.twig' %}

{% block title %}
    {{ "button.editCompany"|trans }}
{% endblock %}

{% block body %}
    <div class="pagetitle">
        <h1>{{ "button.editCompany"|trans }}</h1>
    </div>
    <section class="section">
        <div class="row">
            <div class="card p-4">
                <div class="card-body">
                    {{ include('contact/_form.html.twig', {'button_label': "update"|trans }) }}


            
                </div>
            </div>
        </div>
    </section>
{% endblock %}