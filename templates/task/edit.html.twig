{% extends 'base.html.twig' %}

{% block title %}
    {{ "button.editCompany"|trans }}
{% endblock %}

{% block body %}
    <div class="pagetitle">
        <h1>{{ "button.editCompany"|trans }}</h1>
    </div>
    <section class="section">
        <div class="row">
            <div class="card p-4">
                <div class="card-body">
                    {{ include('task/_form.html.twig', {'button_label': "update"|trans }) }}

                    <a class="btn btn-primary mt-3"
                       href="{{ path('app_task_index', {'id': task.id}) }}">{{ "button.back"|trans }}</a>
            
                </div>
            </div>
        </div>
    </section>
{% endblock %}